# 10: Library shapes
This patch set has been broken up so as to ensure that the patch files are under 50MB each.

## Oxlint
The .oxlintrc.json changes are manually removed from the patch set as they conflict.

