From 2a3f445692fa1c4c75c26c6bf53c1de428be0346 Mon Sep 17 00:00:00 2001
From: Patch Test <patch-test@local>
Date: Tue, 5 May 2026 16:11:52 -0600
Subject: [PATCH 08/10] Further improvements for Library (semi) lazy load

---
 .oxlintrc.json                                |  2 ++
 .../src/components/shape-browser-panel.ts     | 20 ++----------------
 .../affine/gfx/shape/src/drawio/index.ts      |  6 +-----
 .../gfx/shape/src/drawio/stencil-utils.ts     | 21 ++-----------------
 4 files changed, 7 insertions(+), 42 deletions(-)

diff --git a/.oxlintrc.json b/.oxlintrc.json
index cb667d81568fc4bd0d4ac68fdfa9954f74287669..e69669d8e0c0fad5dcd637e83cdb8e17ffb050b9 100644
--- a/.oxlintrc.json
+++ b/.oxlintrc.json
@@ -52,6 +52,8 @@
     "packages/frontend/apps/ios/App/**",
     "tests/blocksuite/snapshots",
     "blocksuite/docs/api/**",
+    "blocksuite/affine/gfx/shape/src/drawio/library-catalog.ts",
+    "blocksuite/affine/gfx/shape/src/drawio/library-stencils.ts",
     "packages/frontend/admin/src/config.json",
     "**/test-docs.json",
     "**/test-blocks.json"
diff --git a/blocksuite/affine/gfx/shape/src/components/shape-browser-panel.ts b/blocksuite/affine/gfx/shape/src/components/shape-browser-panel.ts
index 54838f6fe7b9c521491942d4955a7532d7e94c6e..f6efba8381b0a205532f462c365eb7b31d207bba 100644
--- a/blocksuite/affine/gfx/shape/src/components/shape-browser-panel.ts
+++ b/blocksuite/affine/gfx/shape/src/components/shape-browser-panel.ts
@@ -22,10 +22,9 @@ import { property, state } from 'lit/decorators.js';
 import { repeat } from 'lit/directives/repeat.js';
 import { styleMap } from 'lit/directives/style-map.js';
 
-import type { DrawioLibraryCatalogEntry } from '../drawio/library-catalog';
+import { drawioLibraryCatalog } from '../drawio/library-catalog';
 import {
   buildPathFromStencil,
-  ensureLibraryStencilShapesLoaded,
   getStencilShapeData,
 } from '../drawio/stencil-utils';
 import { AllShapeConfig } from '../toolbar/shape-menu-config';
@@ -510,9 +509,6 @@ export class EdgelessShapeBrowserPanel extends WithDisposable(LitElement) {
   private accessor _viewportWidth =
     typeof window === 'undefined' ? 1440 : window.innerWidth;
 
-  @state()
-  private accessor _libraryCatalog: DrawioLibraryCatalogEntry[] = [];
-
   @property({ attribute: false })
   accessor edgeless!: BlockComponent;
 
@@ -575,7 +571,7 @@ export class EdgelessShapeBrowserPanel extends WithDisposable(LitElement) {
   }
 
   private _getShapeBrowserItems(): ShapeBrowserItem[] {
-    const libraryItems = this._libraryCatalog.map(entry => ({
+    const libraryItems = drawioLibraryCatalog.map(entry => ({
       id: `lib:${entry.id}`,
       name: ShapeType.DrawioStencil,
       tooltip: entry.label,
@@ -586,15 +582,6 @@ export class EdgelessShapeBrowserPanel extends WithDisposable(LitElement) {
     return [...BASE_SHAPE_BROWSER_ITEMS, ...libraryItems];
   }
 
-  private async _loadLibraryShapesData() {
-    const [catalogModule] = await Promise.all([
-      import('../drawio/library-catalog.js'),
-      ensureLibraryStencilShapesLoaded(),
-    ]);
-    this._libraryCatalog = catalogModule.drawioLibraryCatalog;
-    this.requestUpdate();
-  }
-
   private _getFilteredItems(includeSearchableLibraries = true) {
     const items = this._getShapeBrowserItems();
     const visibility = this.edgeless?.std
@@ -722,9 +709,6 @@ export class EdgelessShapeBrowserPanel extends WithDisposable(LitElement) {
 
   override firstUpdated() {
     this._focusSearchInput();
-    this._loadLibraryShapesData().catch(() => {
-      // keep base shapes usable even if library payload fails to load
-    });
 
     const scrollContent = this.renderRoot.querySelector(
       '.shapes-scrollcontent'
diff --git a/blocksuite/affine/gfx/shape/src/drawio/index.ts b/blocksuite/affine/gfx/shape/src/drawio/index.ts
index c25140fb5f2cd5dc6c75b47405ce3b31ddd614d5..d46364169f3891af23f298eb178ab2d49d7e98a5 100644
--- a/blocksuite/affine/gfx/shape/src/drawio/index.ts
+++ b/blocksuite/affine/gfx/shape/src/drawio/index.ts
@@ -1,6 +1,2 @@
 export { DRAWIO_STENCIL_SHAPE_MAP } from './stencil-map';
-export {
-  ensureLibraryStencilShapesLoaded,
-  getStencilShapeData,
-  STENCIL_SHAPE_NAMES,
-} from './stencil-utils';
+export { getStencilShapeData, STENCIL_SHAPE_NAMES } from './stencil-utils';
diff --git a/blocksuite/affine/gfx/shape/src/drawio/stencil-utils.ts b/blocksuite/affine/gfx/shape/src/drawio/stencil-utils.ts
index 94c66c96bd2a5e9e0cd3d24cdcc1ea15af904e82..5a37a138e4a76df6bba3b00a49569c533d54bc30 100644
--- a/blocksuite/affine/gfx/shape/src/drawio/stencil-utils.ts
+++ b/blocksuite/affine/gfx/shape/src/drawio/stencil-utils.ts
@@ -1,3 +1,4 @@
+import { drawioLibraryStencilShapes } from './library-stencils.js';
 import { drawioStencilShapes } from './stencils.js';
 
 export type StencilCommand =
@@ -40,29 +41,11 @@ export type StencilShapeData = {
 
 const allStencilShapes = {
   ...drawioStencilShapes,
+  ...drawioLibraryStencilShapes,
 } as unknown as Record<string, StencilShapeData>;
 
-let libraryShapesLoaded = false;
-let libraryShapesLoading: Promise<void> | null = null;
-
 export const STENCIL_SHAPE_NAMES = Object.keys(allStencilShapes).sort();
 
-export async function ensureLibraryStencilShapesLoaded(): Promise<void> {
-  if (libraryShapesLoaded) return;
-  if (libraryShapesLoading) return libraryShapesLoading;
-
-  libraryShapesLoading = import('./library-stencils.js')
-    .then(mod => {
-      Object.assign(allStencilShapes, mod.drawioLibraryStencilShapes);
-      libraryShapesLoaded = true;
-    })
-    .finally(() => {
-      libraryShapesLoading = null;
-    });
-
-  return libraryShapesLoading;
-}
-
 export const getStencilShapeData = (name: string): StencilShapeData | null =>
   allStencilShapes[name] ?? null;
 
-- 
2.53.0

